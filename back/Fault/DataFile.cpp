#include "DataFile.h"

int32_t DataFile::serialie_data_info(DataInfo::IperfInfo &info)
{
    //info.client_ssh->send_cmd("iperf -ec " + info.server->ip
    //    + string(" -i 1 -p ") + to_string(info.port)
    //    + string(" -t ") + to_string(info.time)
    //    + string(" -b ") + info.bind
    //    + udpcmd + "\n");
    return 0;
}
